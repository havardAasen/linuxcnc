/* Autogenerated by /home/end/projects/linuxcnc/build/bin/halcompile on Thu Sep 28 23:37:50 2023 -- do not edit */
#include "rtapi.h"
#ifdef RTAPI
#include "rtapi_app.h"
#endif
#include "rtapi_string.h"
#include "rtapi_errno.h"
#include "hal.h"
#include "rtapi_math64.h"

static int comp_id;

#ifdef MODULE_INFO
MODULE_INFO(linuxcnc, "component:select8:8-bit binary match detector");
MODULE_INFO(linuxcnc, "pin:enable:bit:0:in:Set enable to FALSE to cause all outputs to be set FALSE.:TRUE:None");
MODULE_INFO(linuxcnc, "pin:sel:s32:0:in:The number of the output to set TRUE.  All other outputs well be set FALSE.:None:None");
MODULE_INFO(linuxcnc, "pin:out#:bit:8:out:Output bits.  If enable is set and the sel input is between 0 and 7, then the corresponding output bit will be set true.:None:None");
MODULE_INFO(linuxcnc, "funct:_:0:");
MODULE_INFO(linuxcnc, "license:GPL");
MODULE_INFO(linuxcnc, "author:Stephen Wille Padnos");
MODULE_LICENSE("GPL");
#endif // MODULE_INFO


struct __comp_state {
    struct __comp_state *_next;
    hal_bit_t *enable_p;
    hal_s32_t *sel_p;
    hal_bit_t *out_p[8];
};
struct __comp_state *__comp_first_inst=0, *__comp_last_inst=0;

static void _(struct __comp_state *__comp_inst, long period);
static int __comp_get_data_size(void);
#undef TRUE
#define TRUE (1)
#undef FALSE
#define FALSE (0)
#undef true
#define true (1)
#undef false
#define false (0)

static int export(char *prefix, long extra_arg) {
    char buf[HAL_NAME_LEN + 1];
    int r = 0;
    int j = 0;
    int sz = sizeof(struct __comp_state) + __comp_get_data_size();
    struct __comp_state *inst = hal_malloc(sz);
    memset(inst, 0, sz);
    r = hal_pin_bit_newf(HAL_IN, &(inst->enable_p), comp_id,
        "%s.enable", prefix);
    if(r != 0) return r;
    *(inst->enable_p) = TRUE;
    r = hal_pin_s32_newf(HAL_IN, &(inst->sel_p), comp_id,
        "%s.sel", prefix);
    if(r != 0) return r;
    for(j=0; j < (8); j++) {
        r = hal_pin_bit_newf(HAL_OUT, &(inst->out_p[j]), comp_id,
            "%s.out%01d", prefix, j);
        if(r != 0) return r;
    }
    rtapi_snprintf(buf, sizeof(buf), "%s", prefix);
    r = hal_export_funct(buf, (void(*)(void *inst, long))_, inst, 0, 0, comp_id);
    if(r != 0) return r;
    if(__comp_last_inst) __comp_last_inst->_next = inst;
    __comp_last_inst = inst;
    if(!__comp_first_inst) __comp_first_inst = inst;
    return 0;
}
static int default_count=1, count=0;
RTAPI_MP_INT(count, "number of select8");
char *names = ""; // comma separated names
RTAPI_MP_STRING(names, "names of select8");
int rtapi_app_main(void) {
    int r = 0;
    int i;
    comp_id = hal_init("select8");
    if(comp_id < 0) return comp_id;
    if(count && names[0]) {
        rtapi_print_msg(RTAPI_MSG_ERR,"count= and names= are mutually exclusive\n");
        return -EINVAL;
    }
    if(!count && !names[0]) count = default_count;
    if(count) {
        for(i=0; i<count; i++) {
            char buf[HAL_NAME_LEN + 1];
            rtapi_snprintf(buf, sizeof(buf), "select8.%d", i);
            r = export(buf, i);
            if(r != 0) break;
       }
    } else {
        size_t i, j;
        int idx;
        char buf[HAL_NAME_LEN+1];
        const size_t length = strlen(names);
        for (i = j = idx = 0; i <= length; i++) {
            const char c = buf[j] = names[i];
            if ((c == ',') || (c == '\0')) {
                buf[j] = '\0';
                r = export(buf, idx);
                if(r != 0) {break;}
                idx++;
                j = 0;
            } else {
                if (++j == (sizeof(buf) / sizeof(buf[0]))) {
                    buf[j - 1] = '\0';
                    rtapi_print_msg(RTAPI_MSG_ERR,"names: \"%s\" too long\n", buf);
                    r = -EINVAL;
                    break;
                }
            }
        }
    }
    if(r) {
        hal_exit(comp_id);
    } else {
        hal_ready(comp_id);
    }
    return r;
}

void rtapi_app_exit(void) {
    hal_exit(comp_id);
}

#undef FUNCTION
#define FUNCTION(name) static void name(struct __comp_state *__comp_inst, long period)
#undef EXTRA_SETUP
#define EXTRA_SETUP() static int extra_setup(struct __comp_state *__comp_inst, char *prefix, long extra_arg)
#undef EXTRA_CLEANUP
#define EXTRA_CLEANUP() static void extra_cleanup(void)
#undef fperiod
#define fperiod (period * 1e-9)
#undef enable
#undef enable_ptr
#define enable_ptr (__comp_inst->enable_p)
#define enable (0+*__comp_inst->enable_p)
#undef sel
#undef sel_ptr
#define sel_ptr (__comp_inst->sel_p)
#define sel (0+*__comp_inst->sel_p)
#undef out
#undef out_ptr
#define out_ptr(i) (__comp_inst->out_p[i])
#define out(i) (*(__comp_inst->out_p[i]))


#line 9 "/home/end/projects/linuxcnc/src/hal/components/select8.comp"
FUNCTION(_) {
	hal_s32_t temp_sel;
	int i, temp_enable;
	temp_sel = sel;
	temp_enable = enable;
	for (i=0;i<8;i++) {
		if (!temp_enable || temp_sel!=i)
			out(i)=0;
		else
			out(i)=1;
	}
}

static int __comp_get_data_size(void) { return 0; }
