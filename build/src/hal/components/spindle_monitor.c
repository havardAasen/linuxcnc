/* Autogenerated by /home/end/projects/linuxcnc/build/bin/halcompile on Thu Sep 28 23:37:50 2023 -- do not edit */
#include "rtapi.h"
#ifdef RTAPI
#include "rtapi_app.h"
#endif
#include "rtapi_string.h"
#include "rtapi_errno.h"
#include "hal.h"
#include "rtapi_math64.h"

static int comp_id;

#ifdef MODULE_INFO
MODULE_INFO(linuxcnc, "component:spindle_monitor:spindle at-speed and underspeed detection");
MODULE_INFO(linuxcnc, "pin:spindle-is-on:bit:0:in::None:None");
MODULE_INFO(linuxcnc, "pin:spindle-command:float:0:in::None:None");
MODULE_INFO(linuxcnc, "pin:spindle-feedback:float:0:in::None:None");
MODULE_INFO(linuxcnc, "pin:spindle-at-speed:bit:0:out::None:None");
MODULE_INFO(linuxcnc, "pin:spindle-underspeed:bit:0:out::None:None");
MODULE_INFO(linuxcnc, "param:level:u32:0:rw:state machine state:None:None");
MODULE_INFO(linuxcnc, "param:threshold:float:0:rw::None:None");
MODULE_INFO(linuxcnc, "funct:_:1:");
MODULE_INFO(linuxcnc, "license:gpl v2 or higher");
MODULE_INFO(linuxcnc, "author:Sebastian Kuzminsky");
MODULE_LICENSE("gpl v2 or higher");
#endif // MODULE_INFO


struct __comp_state {
    struct __comp_state *_next;
    hal_bit_t *spindle_is_on_p;
    hal_float_t *spindle_command_p;
    hal_float_t *spindle_feedback_p;
    hal_bit_t *spindle_at_speed_p;
    hal_bit_t *spindle_underspeed_p;
    hal_u32_t level_p;
    hal_float_t threshold_p;
};
struct __comp_state *__comp_first_inst=0, *__comp_last_inst=0;

static void _(struct __comp_state *__comp_inst, long period);
static int __comp_get_data_size(void);
#undef TRUE
#define TRUE (1)
#undef FALSE
#define FALSE (0)
#undef true
#define true (1)
#undef false
#define false (0)

static int export(char *prefix, long extra_arg) {
    char buf[HAL_NAME_LEN + 1];
    int r = 0;
    int sz = sizeof(struct __comp_state) + __comp_get_data_size();
    struct __comp_state *inst = hal_malloc(sz);
    memset(inst, 0, sz);
    r = hal_pin_bit_newf(HAL_IN, &(inst->spindle_is_on_p), comp_id,
        "%s.spindle-is-on", prefix);
    if(r != 0) return r;
    r = hal_pin_float_newf(HAL_IN, &(inst->spindle_command_p), comp_id,
        "%s.spindle-command", prefix);
    if(r != 0) return r;
    r = hal_pin_float_newf(HAL_IN, &(inst->spindle_feedback_p), comp_id,
        "%s.spindle-feedback", prefix);
    if(r != 0) return r;
    r = hal_pin_bit_newf(HAL_OUT, &(inst->spindle_at_speed_p), comp_id,
        "%s.spindle-at-speed", prefix);
    if(r != 0) return r;
    r = hal_pin_bit_newf(HAL_OUT, &(inst->spindle_underspeed_p), comp_id,
        "%s.spindle-underspeed", prefix);
    if(r != 0) return r;
    r = hal_param_u32_newf(HAL_RW, &(inst->level_p), comp_id,
        "%s.level", prefix);
    if(r != 0) return r;
    r = hal_param_float_newf(HAL_RW, &(inst->threshold_p), comp_id,
        "%s.threshold", prefix);
    if(r != 0) return r;
    rtapi_snprintf(buf, sizeof(buf), "%s", prefix);
    r = hal_export_funct(buf, (void(*)(void *inst, long))_, inst, 1, 0, comp_id);
    if(r != 0) return r;
    if(__comp_last_inst) __comp_last_inst->_next = inst;
    __comp_last_inst = inst;
    if(!__comp_first_inst) __comp_first_inst = inst;
    return 0;
}
static int default_count=1, count=0;
RTAPI_MP_INT(count, "number of spindle_monitor");
char *names = ""; // comma separated names
RTAPI_MP_STRING(names, "names of spindle_monitor");
int rtapi_app_main(void) {
    int r = 0;
    int i;
    comp_id = hal_init("spindle_monitor");
    if(comp_id < 0) return comp_id;
    if(count && names[0]) {
        rtapi_print_msg(RTAPI_MSG_ERR,"count= and names= are mutually exclusive\n");
        return -EINVAL;
    }
    if(!count && !names[0]) count = default_count;
    if(count) {
        for(i=0; i<count; i++) {
            char buf[HAL_NAME_LEN + 1];
            rtapi_snprintf(buf, sizeof(buf), "spindle-monitor.%d", i);
            r = export(buf, i);
            if(r != 0) break;
       }
    } else {
        size_t i, j;
        int idx;
        char buf[HAL_NAME_LEN+1];
        const size_t length = strlen(names);
        for (i = j = idx = 0; i <= length; i++) {
            const char c = buf[j] = names[i];
            if ((c == ',') || (c == '\0')) {
                buf[j] = '\0';
                r = export(buf, idx);
                if(r != 0) {break;}
                idx++;
                j = 0;
            } else {
                if (++j == (sizeof(buf) / sizeof(buf[0]))) {
                    buf[j - 1] = '\0';
                    rtapi_print_msg(RTAPI_MSG_ERR,"names: \"%s\" too long\n", buf);
                    r = -EINVAL;
                    break;
                }
            }
        }
    }
    if(r) {
        hal_exit(comp_id);
    } else {
        hal_ready(comp_id);
    }
    return r;
}

void rtapi_app_exit(void) {
    hal_exit(comp_id);
}

#undef FUNCTION
#define FUNCTION(name) static void name(struct __comp_state *__comp_inst, long period)
#undef EXTRA_SETUP
#define EXTRA_SETUP() static int extra_setup(struct __comp_state *__comp_inst, char *prefix, long extra_arg)
#undef EXTRA_CLEANUP
#define EXTRA_CLEANUP() static void extra_cleanup(void)
#undef fperiod
#define fperiod (period * 1e-9)
#undef spindle_is_on
#undef spindle_is_on_ptr
#define spindle_is_on_ptr (__comp_inst->spindle_is_on_p)
#define spindle_is_on (0+*__comp_inst->spindle_is_on_p)
#undef spindle_command
#undef spindle_command_ptr
#define spindle_command_ptr (__comp_inst->spindle_command_p)
#define spindle_command (0+*__comp_inst->spindle_command_p)
#undef spindle_feedback
#undef spindle_feedback_ptr
#define spindle_feedback_ptr (__comp_inst->spindle_feedback_p)
#define spindle_feedback (0+*__comp_inst->spindle_feedback_p)
#undef spindle_at_speed
#undef spindle_at_speed_ptr
#define spindle_at_speed_ptr (__comp_inst->spindle_at_speed_p)
#define spindle_at_speed (*__comp_inst->spindle_at_speed_p)
#undef spindle_underspeed
#undef spindle_underspeed_ptr
#define spindle_underspeed_ptr (__comp_inst->spindle_underspeed_p)
#define spindle_underspeed (*__comp_inst->spindle_underspeed_p)
#undef level
#define level (__comp_inst->level_p)
#undef threshold
#define threshold (__comp_inst->threshold_p)


#line 17 "/home/end/projects/linuxcnc/src/hal/components/spindle_monitor.comp"

#include <rtapi_math.h>

FUNCTION(_) {

switch (level){
    case 0:  // idle
        spindle_at_speed = 0;
        spindle_underspeed = 0;
        if (spindle_is_on) level = 1;
        break;
    case 1: // waiting for spindle-at-speed
        if ( ! spindle_is_on ) {
            level = 0;
            return; }
        if (fabs(spindle_command - spindle_feedback) < threshold) {
            level = 2;
            spindle_at_speed = 1;
            return; }
        break;
    case 2: // monitoring speed
        if ( ! spindle_is_on ) {
            level = 0;
            return; }
        if  ((spindle_command - spindle_feedback) > threshold) {
            spindle_underspeed = 1; }
        break;
    default:
        // not sure how we got here, but fix the situation
        level = 0;
    }
}

static int __comp_get_data_size(void) { return 0; }
