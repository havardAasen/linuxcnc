/* Autogenerated by /home/end/projects/linuxcnc/build/bin/halcompile on Thu Sep 28 23:37:49 2023 -- do not edit */
#include "rtapi.h"
#ifdef RTAPI
#include "rtapi_app.h"
#endif
#include "rtapi_string.h"
#include "rtapi_errno.h"
#include "hal.h"
#include "rtapi_math64.h"

static int comp_id;

#ifdef MODULE_INFO
MODULE_INFO(linuxcnc, "component:clarkeinv:Inverse Clarke transform");
MODULE_INFO(linuxcnc, "descr:The inverse Clarke transform can be used rotate a \nvector quantity and then translate it from Cartesian coordinate\nsystem to a three phase system (three components 120 degrees apart).");
MODULE_INFO(linuxcnc, "see_also:\\fBclarke2\\fR(9) and \\fBclarke3\\fR(9) for the forward transform.");
MODULE_INFO(linuxcnc, "pin:x:float:0:in::None:None");
MODULE_INFO(linuxcnc, "pin:y:float:0:in:cartesian components of input:None:None");
MODULE_INFO(linuxcnc, "pin:h:float:0:in:homopolar component of input (usually zero):None:None");
MODULE_INFO(linuxcnc, "pin:theta:float:0:in:rotation angle: 0.00 to 1.00 = 0 to 360 degrees:None:None");
MODULE_INFO(linuxcnc, "pin:a:float:0:out::None:None");
MODULE_INFO(linuxcnc, "pin:b:float:0:out::None:None");
MODULE_INFO(linuxcnc, "pin:c:float:0:out:three phase output vector:None:None");
MODULE_INFO(linuxcnc, "funct:_:1:");
MODULE_INFO(linuxcnc, "license:GPL");
MODULE_INFO(linuxcnc, "author:John Kasunich");
MODULE_LICENSE("GPL");
#endif // MODULE_INFO


struct __comp_state {
    struct __comp_state *_next;
    hal_float_t *x_p;
    hal_float_t *y_p;
    hal_float_t *h_p;
    hal_float_t *theta_p;
    hal_float_t *a_p;
    hal_float_t *b_p;
    hal_float_t *c_p;
};
struct __comp_state *__comp_first_inst=0, *__comp_last_inst=0;

static void _(struct __comp_state *__comp_inst, long period);
static int __comp_get_data_size(void);
#undef TRUE
#define TRUE (1)
#undef FALSE
#define FALSE (0)
#undef true
#define true (1)
#undef false
#define false (0)

static int export(char *prefix, long extra_arg) {
    char buf[HAL_NAME_LEN + 1];
    int r = 0;
    int sz = sizeof(struct __comp_state) + __comp_get_data_size();
    struct __comp_state *inst = hal_malloc(sz);
    memset(inst, 0, sz);
    r = hal_pin_float_newf(HAL_IN, &(inst->x_p), comp_id,
        "%s.x", prefix);
    if(r != 0) return r;
    r = hal_pin_float_newf(HAL_IN, &(inst->y_p), comp_id,
        "%s.y", prefix);
    if(r != 0) return r;
    r = hal_pin_float_newf(HAL_IN, &(inst->h_p), comp_id,
        "%s.h", prefix);
    if(r != 0) return r;
    r = hal_pin_float_newf(HAL_IN, &(inst->theta_p), comp_id,
        "%s.theta", prefix);
    if(r != 0) return r;
    r = hal_pin_float_newf(HAL_OUT, &(inst->a_p), comp_id,
        "%s.a", prefix);
    if(r != 0) return r;
    r = hal_pin_float_newf(HAL_OUT, &(inst->b_p), comp_id,
        "%s.b", prefix);
    if(r != 0) return r;
    r = hal_pin_float_newf(HAL_OUT, &(inst->c_p), comp_id,
        "%s.c", prefix);
    if(r != 0) return r;
    rtapi_snprintf(buf, sizeof(buf), "%s", prefix);
    r = hal_export_funct(buf, (void(*)(void *inst, long))_, inst, 1, 0, comp_id);
    if(r != 0) return r;
    if(__comp_last_inst) __comp_last_inst->_next = inst;
    __comp_last_inst = inst;
    if(!__comp_first_inst) __comp_first_inst = inst;
    return 0;
}
static int default_count=1, count=0;
RTAPI_MP_INT(count, "number of clarkeinv");
char *names = ""; // comma separated names
RTAPI_MP_STRING(names, "names of clarkeinv");
int rtapi_app_main(void) {
    int r = 0;
    int i;
    comp_id = hal_init("clarkeinv");
    if(comp_id < 0) return comp_id;
    if(count && names[0]) {
        rtapi_print_msg(RTAPI_MSG_ERR,"count= and names= are mutually exclusive\n");
        return -EINVAL;
    }
    if(!count && !names[0]) count = default_count;
    if(count) {
        for(i=0; i<count; i++) {
            char buf[HAL_NAME_LEN + 1];
            rtapi_snprintf(buf, sizeof(buf), "clarkeinv.%d", i);
            r = export(buf, i);
            if(r != 0) break;
       }
    } else {
        size_t i, j;
        int idx;
        char buf[HAL_NAME_LEN+1];
        const size_t length = strlen(names);
        for (i = j = idx = 0; i <= length; i++) {
            const char c = buf[j] = names[i];
            if ((c == ',') || (c == '\0')) {
                buf[j] = '\0';
                r = export(buf, idx);
                if(r != 0) {break;}
                idx++;
                j = 0;
            } else {
                if (++j == (sizeof(buf) / sizeof(buf[0]))) {
                    buf[j - 1] = '\0';
                    rtapi_print_msg(RTAPI_MSG_ERR,"names: \"%s\" too long\n", buf);
                    r = -EINVAL;
                    break;
                }
            }
        }
    }
    if(r) {
        hal_exit(comp_id);
    } else {
        hal_ready(comp_id);
    }
    return r;
}

void rtapi_app_exit(void) {
    hal_exit(comp_id);
}

#undef FUNCTION
#define FUNCTION(name) static void name(struct __comp_state *__comp_inst, long period)
#undef EXTRA_SETUP
#define EXTRA_SETUP() static int extra_setup(struct __comp_state *__comp_inst, char *prefix, long extra_arg)
#undef EXTRA_CLEANUP
#define EXTRA_CLEANUP() static void extra_cleanup(void)
#undef fperiod
#define fperiod (period * 1e-9)
#undef x
#undef x_ptr
#define x_ptr (__comp_inst->x_p)
#define x (0+*__comp_inst->x_p)
#undef y
#undef y_ptr
#define y_ptr (__comp_inst->y_p)
#define y (0+*__comp_inst->y_p)
#undef h
#undef h_ptr
#define h_ptr (__comp_inst->h_p)
#define h (0+*__comp_inst->h_p)
#undef theta
#undef theta_ptr
#define theta_ptr (__comp_inst->theta_p)
#define theta (0+*__comp_inst->theta_p)
#undef a
#undef a_ptr
#define a_ptr (__comp_inst->a_p)
#define a (*__comp_inst->a_p)
#undef b
#undef b_ptr
#define b_ptr (__comp_inst->b_p)
#define b (*__comp_inst->b_p)
#undef c
#undef c_ptr
#define c_ptr (__comp_inst->c_p)
#define c (*__comp_inst->c_p)


#line 17 "/home/end/projects/linuxcnc/src/hal/components/clarkeinv.comp"
#include <rtapi_math.h>

/* for the details, google "clarke transform", or see section 3 of
   http://focus.ti.com/lit/an/bpra048/bpra048.pdf and/or appendix B of
   http://www.esat.kuleuven.be/electa/publications/fulltexts/pub_1610.pdf
*/

#define K1 (0.500000000000000)  /* 1/2       */
#define K2 (0.866025403784439)  /* sqrt(3)/2 */
#define K3 (0.707106781186547)  /* 1/sqrt(2) */
#define rev2rad (6.283185308)   /* 2 * pi */

FUNCTION(_) {
    double xr, yr, rads, costheta, sintheta;

    // rotate it
    rads = theta * rev2rad;
    costheta = cos(rads);
    sintheta = sin(rads);
    xr = x * costheta - y * sintheta;
    yr = x * sintheta + y * costheta;

    /* transform to three phase */
    a =  xr +  K3*h;
    b = -K1*xr + K2*yr + K3*h;
    c = -K1*xr - K2*yr + K3*h;
}

static int __comp_get_data_size(void) { return 0; }
